# Experiments

실행 가능한 실험 스크립트와 raw 결과를 남긴다.

예:

```text
experiments/
└── sync-baseline/
    ├── k6.js
    └── results/
```

실험의 해석, 회고, 다음 결정은 `docs/notes/`에 남긴다.

블로그에 옮길 재료는 `docs/blog/`에 별도로 정리한다.
